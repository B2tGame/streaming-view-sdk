export default {
  preferH264: true,

  // WebRTC bitrates
  // These value are just the Chrome defaults, we haven't done much investigation on them.
  // They are all in Kbps.
  startBitrate: 300,
  minBitrate: 30,
  maxBitrate: 2000,
};
